#include <iostream>
#include <fstream>
using namespace std;

int main()
{
  ofstream outputf
  int EmployeeID, num; // to hold for how many employee to process.
  string EmployeeName, DepartmentName;
  double Salary;
  //Open the file
  outputf.open("employee.txt");
  cout << "Enter the number of employees\n";
  cin << num;
  for(int i = 0; i < num; i++ )
  
}